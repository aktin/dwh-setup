TODO hier alle Schritte beschreiben, die nötig sind um das Zip-Archiv 
für das Update zu erstellen.


Z.B. 
1. dwh-j2ee-0.6-SNAPSHOT.ear in packages kopieren
2. tar cvfz update_0.6.tar.gz packages scripts *.sh *.conf

-- per mvn clean install -> in target das tar.gz
--> auf der seite umbauen in tar.gz

Außerdem den Text für die Rundmail auch hier in den Ordner legen (EMAIL.txt)

-- done